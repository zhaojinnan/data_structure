1.顺序结构可以不用创建空间，但链式结构需要创建空间。顺序结构不能用指针的形式来初始化，链式结构可以用指针的形式来初始化。
2.设statement是用户输入的表达式，通过一位一位的解析这个字符串来实现计算，假设当前的字符是c，具体的规则是

（1）如果c='+'或者’-‘，那么当符号栈非空并且栈顶元素不为’（‘时，计算一次，计算完成后再把符号入栈。
（2）如果c='*'或者'/'，那么当符号栈非空并且栈顶元素为’*‘或者'/'时，计算一次，计算完成后把符号入栈。
（3）如果c是数字，那么入数字栈。
（4）如果c是’（‘，那么直接入符号栈。
（5）如果c是’）‘，那么一直计算到栈顶为'('为止，最后再把’(‘弹出栈